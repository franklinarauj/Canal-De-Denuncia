import { StringMap } from '@angular/compiler/src/compiler_facade_interface';

export interface Product {
    id: string
    name: string
    category: string
    department: string
    date: string
    email: string
    contact: string
    description: string
}